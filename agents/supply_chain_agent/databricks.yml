bundle:
  name: dbai-supply-chain-contract-ka-agent
  engine: direct

include:
  - resources/*.yml

sync:
  include:
    - src/**
    - static/**
    - pyproject.toml
    - app.yaml
  exclude:
    - .databricks/**
    - README.md
    - databricks.yml
    - resources/**
    - tests/**
    - '**/__pycache__/**'
    - '**/*.pyc'

targets:
  dev:
    mode: development
    default: true
  test:
    mode: production
  prod:
    mode: production
