# Agents

Each agent owns its prompt, tools, runtime package, tests, App configuration, and Databricks Asset Bundle boundary.

Shared ingestion, Unity Catalog data contracts, AI Search provisioning, and platform jobs remain outside an agent package until an agent-specific owner is required.
